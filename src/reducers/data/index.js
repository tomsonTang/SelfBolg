
import CONSTANT from "../../constant";

const xxxInitialState = {
};

const dataReducer = (state = xxxInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cacheReducer;