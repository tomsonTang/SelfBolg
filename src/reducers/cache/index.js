
import CONSTANT from "../../constant";

const xxxInitialState = {
};

const cacheReducer = (state = xxxInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cacheReducer;