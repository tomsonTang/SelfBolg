
import CONSTANT from "../../constant";

const xxxInitialState = {
};

const responseReducer = (state = xxxInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default responseReducer;